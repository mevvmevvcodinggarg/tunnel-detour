import AppKit

enum TunnelDetourTheme {
    static let sidebarWidth: CGFloat = 260
    static let contentInset: CGFloat = 20
    static let sectionSpacing: CGFloat = 16
    static let titleFont = NSFont.systemFont(ofSize: 20, weight: .semibold)
    static let sectionFont = NSFont.systemFont(ofSize: 13, weight: .semibold)
    static let compactFont = NSFont.systemFont(ofSize: 12, weight: .medium)
}
